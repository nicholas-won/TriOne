{
  "name": "trione",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "npm --workspace @trione/api",
    "dev:api": "npm --workspace @trione/api run dev",
    "build:api": "npm --workspace @trione/api run build",
    "seed": "npm --workspace @trione/api run seed"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
